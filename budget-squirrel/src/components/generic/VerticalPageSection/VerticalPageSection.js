import React from 'react'
import './VerticalPageSection.scss';

export default function VerticalPageSection({ children }) {
    return (
        <div class="vert-section">
            {children}
        </div>
    )
}
